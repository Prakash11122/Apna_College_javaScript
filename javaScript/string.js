let msg = "hello";

let newMsg = msg.trim();
console.log("after triming :", newMsg);
let newmsg = msg.toUpperCase();
console.log("after uppercase:", newmsg)

let newcase = msg.trim().toUpperCase();
console.log(newcase);
